name: Update Baltimore Comedy site

# Runs automatically every morning, and any time you push to main.
# You can also trigger it by hand from the Actions tab (Run workflow)
# right after approving something in a sheet, if you don't want to wait
# for the schedule.
on:
  schedule:
    - cron: "0 12 * * *"   # 8am US Eastern (UTC-4) — adjust if needed
  workflow_dispatch: {}
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Pull approved rows and regenerate content
        run: python3 scripts/generate_content.py

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Render site into docs/
        run: quarto render

      - name: Tell GitHub Pages to skip Jekyll processing
        run: touch docs/.nojekyll

      - name: Commit regenerated content and rendered site
        run: |
          git config user.name "baltimore-comedy-bot"
          git config user.email "actions@users.noreply.github.com"
          git add _generated docs
          git diff --cached --quiet || git commit -m "Auto-update site content [skip ci]"
          git push
